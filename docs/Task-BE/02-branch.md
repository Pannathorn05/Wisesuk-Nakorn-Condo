# module branch (T-23 – T-31)

ขอบเขตโค้ด: backend/internal/modules/branch
อ้างอิง SPEC: AC-26
เทสที่มีแล้ว: closed_test.go

---

T-23 · Test: แก้รายละเอียดสาขา (AC-26)
- ทำ: branch/update_test.go
- TC
  - admin ไม่ระบุสาขา → แก้สาขาตัวเองสำเร็จ ได้สาขาพร้อมรูป สิ่งอำนวยความสะดวก และสถานที่ใกล้เคียง
  - admin ระบุสาขาอื่น → ไม่มีสิทธิ์ · super admin ไม่ระบุ → "กรุณาระบุสาขา (branch_id)" · รหัสสาขาผิดรูปแบบ → คำขอผิดรูปแบบ
  - ชื่อว่างหรือยาว 201 ตัว → ข้อมูลไม่ถูกต้อง · จำนวนอาคารเป็น 0 → ข้อมูลไม่ถูกต้อง · ค่าน้ำติดลบ → ข้อมูลไม่ถูกต้อง
  - ราคารายเดือนต่ำสุด 5,000 สูงสุด 4,000 → ข้อมูลไม่ถูกต้องที่ช่องราคาสูงสุด
  - ไม่ส่งพิกัด → ค่าเดิมถูกล้าง · ไม่ส่งรูปปก → คงเดิม · รูปปกเป็นลิงก์ javascript → ข้อมูลไม่ถูกต้อง
  - ส่งรายการเบอร์ว่าง → เบอร์ถูกล้าง
  - สำเร็จ → มีบันทึกผูกกับสาขานั้น
- ขึ้นกับ: —
- DoD: TEST(modules/branch, TestUpdateBranch) เขียว · ถ้าแดงเพราะโค้ดไม่ตรง SPEC ให้หยุดแล้วแตก task ใหม่

T-24 · Test: ไม่ส่งรายการเบอร์ (AC-26 ข้อ [ช่องว่าง] [ยังไม่ยืนยัน])
- TC: แก้สาขาโดยไม่มีรายการเบอร์เลย หรือส่งเป็นค่าว่างเปล่า → สำเร็จ และเบอร์เป็นรายการว่าง
- ขึ้นกับ: —
- DoD: แดง (คาดว่าเกิดข้อผิดพลาดภายในระบบ) → ไป T-25 · ถ้าเขียวอยู่แล้ว ให้ตัดป้ายใน SPEC แล้วปิด T-25 โดยไม่ต้องทำ

T-25 · Implement: รายการเบอร์ที่ไม่ส่งมาถือเป็นรายการว่าง
- ทำ: service แก้สาขาแปลงรายการเบอร์ที่ไม่มีค่าเป็นรายการว่างก่อนบันทึก
- ขึ้นกับ: T-24
- DoD: T-24 เขียว · ลบหมายเหตุใน openapi · ตัดป้ายใน SPEC · CHECK สะอาด

T-26 · Test: ค่าเงินของสาขาติดลบไม่ได้ (AC-26 ข้อ [ช่องว่าง])
- TC
  - ค่าทำสัญญา เงินประกัน หรือค่าล่วงหน้าติดลบ → ข้อมูลไม่ถูกต้องที่ช่องนั้น
  - ใส่ค่าทำสัญญาติดลบตรงเข้าฐานข้อมูล → ฐานข้อมูลปฏิเสธ
- ขึ้นกับ: —
- DoD: TEST(modules/branch, TestBranchMoneyNotNegative) แดง

T-27 · Implement: ตรวจค่าเงินและเพิ่มกฎที่ฐานข้อมูล
- ทำ: service ตรวจไม่ติดลบทั้งสามช่อง · migration เพิ่มกฎห้ามติดลบของค่าทำสัญญา เงินประกัน ค่าล่วงหน้า ค่าน้ำ ค่าไฟ (ปรับแถวที่ติดลบอยู่เป็น 0 ใน migration เดียวกัน) ตาม AGENTS.md ข้อ 4
- ขึ้นกับ: T-26
- DoD: T-26 เขียว · แก้ openapi และ SPEC AC-26 · CHECK สะอาด

T-28 · Test: ตั้งสิ่งอำนวยความสะดวกและสถานที่ใกล้เคียงต้องสำเร็จทั้งชุดหรือไม่เปลี่ยนเลย (AC-26 ข้อ [ช่องว่าง])
- TC
  - สาขามี 3 รายการ → ตั้งด้วยรหัสที่ไม่มีจริง → "มีสิ่งอำนวยความสะดวกที่ไม่มีอยู่ในระบบ" และ 3 รายการเดิมยังอยู่
  - ตั้งด้วยรหัสซ้ำในรายการ → สำเร็จ ได้รายการเดียว
  - สถานที่ใกล้เคียงมี 5 รายการ → ตั้งชุดใหม่ที่พังกลางทาง → 5 รายการเดิมยังอยู่
- ขึ้นกับ: —
- DoD: TEST(modules/branch, TestBranchReplaceIsAtomic) แดง

T-29 · Implement: ทำในธุรกรรมเดียวและแปลงข้อผิดพลาด
- ทำ: ส่งตัวจัดการธุรกรรมเข้า branch service · ตั้งสิ่งอำนวยความสะดวกและสถานที่ใกล้เคียงในธุรกรรมเดียว · ตัดรหัสซ้ำ · แปลงการอ้างรหัสที่ไม่มีจริงเป็นข้อมูลไม่ถูกต้อง (แบบเดียวกับของห้อง)
- ขึ้นกับ: T-28
- DoD: T-28 เขียว · แก้ openapi · ตัดป้ายใน SPEC AC-26 · CHECK สะอาด

T-30 · ⚖ ต้องตัดสินใจ + Test: ตรวจรูปแบบอีเมล เบอร์ และลิงก์แผนที่ของสาขา (AC-26 ข้อ [ช่องว่าง])
- ข้อเสนอ: อีเมลใช้กติกาเดียวกับการสมัคร (ว่างได้) · เบอร์แต่ละเบอร์ใช้กติกาเบอร์เดิม · ลิงก์แผนที่ต้องเป็น http หรือ https (ว่างได้)
- TC: อีเมล abc → ข้อมูลไม่ถูกต้อง · เบอร์ abc → ข้อมูลไม่ถูกต้อง · ลิงก์แผนที่ javascript → ข้อมูลไม่ถูกต้อง · ค่าว่างทั้งหมด → สำเร็จ
- ขึ้นกับ: การตัดสินใจ
- DoD: TEST(modules/branch, TestBranchContactFormat) แดง

T-31 · Implement: ตรวจรูปแบบข้อมูลติดต่อของสาขา
- ทำ: service แก้สาขาเพิ่มการตรวจตามที่ตกลง
- ขึ้นกับ: T-30
- DoD: T-30 เขียว · T-23 ยังเขียว · แก้ openapi และ SPEC AC-26 · CHECK สะอาด
