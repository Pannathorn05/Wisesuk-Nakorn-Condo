# ของกลางที่ทุก module ใช้ (T-01 – T-06)

ขอบเขตโค้ด: backend/internal/httpx, middleware, storage, config, shared/access, routes
อ้างอิง SPEC: AC-4, AC-14, AC-15, AC-22

---

T-01 · Test: ความทนทานพื้นฐาน (AC-15)
- ทำ: เทสผ่าน router จริงในไฟล์ใหม่ backend/internal/routes/resilience_test.go
- TC
  - ไม่ส่งรหัสติดตามคำขอ → คำตอบมีรหัสที่ระบบสุ่มให้ · ส่ง abc มา → ได้ abc กลับ
  - เรียกข้ามโดเมนจาก localhost:3000 → อนุญาตพร้อม credentials · จากโดเมนอื่น → ไม่อนุญาต
  - ข้อมูลใหญ่เกิน 1MB → คำขอผิดรูปแบบ · ข้อมูลซ้อนสองชุด → คำขอผิดรูปแบบ
  - เปิดโฟลเดอร์ไฟล์ (uploads และ uploads/slips) เพื่อดูรายชื่อ → ไม่พบข้อมูล
  - ไม่ตั้งที่อยู่ฐานข้อมูล หรือรหัสลับ token ยาว 31 ตัว → โหลดค่าตั้งค่าไม่ผ่าน
  - ข้อผิดพลาดภายในระบบทุกกรณี → ข้อความ "เกิดข้อผิดพลาดภายในระบบ" ไม่มีข้อความ SQL
- ขึ้นกับ: —
- DoD: TEST(routes, TestResilience) เขียว · ถ้าแดงเพราะโค้ดไม่ตรง SPEC ให้หยุดแล้วแตก task ใหม่ · CHECK สะอาด

T-02 · Test: ยืนยันรูปแบบเวลาที่ส่งกลับ (AC-22 ข้อ [ยังไม่ยืนยัน])
- ทำ: เทสที่สร้างใบจองรายวันแล้วอ่านค่าวันเข้าพักและเวลาสร้างแบบดิบ
- TC
  - บันทึกผลจริงของวันเข้าพัก (คาดว่าเป็นเที่ยงคืนเวลาสากล) และเขตเวลาของเวลาสร้าง เมื่อรันด้วยเขตเวลาสากลเทียบกับเวลาไทย
- ขึ้นกับ: —
- DoD: เทสเขียวตามผลจริง และแก้ SPEC AC-22 ตัดป้าย [ยังไม่ยืนยัน] · ถ้า human อยากได้วันที่เปล่า ให้แตก task ใหม่ (กระทบ frontend และ openapi)

T-03 · Test: คำขอรูปที่ถูกปฏิเสธต้องไม่ทิ้งไฟล์ (AC-14 ข้อ [ช่องว่าง] ไฟล์กำพร้า)
- ทำ: เทสใน backend/internal/storage ที่นับไฟล์ในฐานข้อมูลก่อนและหลังคำขอ
- TC
  - admin สาขา A อัปรูปแกลเลอรีให้สาขา B → ไม่มีสิทธิ์ และจำนวนไฟล์ไม่เพิ่ม
  - admin สาขา A อัปรูปปกห้องของสาขา B → ไม่มีสิทธิ์ และจำนวนไฟล์ไม่เพิ่ม
  - อัปรูปให้ห้องที่ไม่มีจริง → ไม่พบข้อมูล และจำนวนไฟล์ไม่เพิ่ม
  - ลำดับรูปเป็นตัวอักษร → คำขอผิดรูปแบบ และจำนวนไฟล์ไม่เพิ่ม
- ขึ้นกับ: —
- DoD: TEST(storage, TestUploadRejectedWritesNoAsset) แดง เพราะจำนวนไฟล์เพิ่ม

T-04 · Implement: ตรวจสิทธิ์และช่องข้อความก่อนเก็บไฟล์รูป
- ทำ: handler อัปรูปของ branch (รูปปก, แกลเลอรี) และ room (รูปปก, แกลเลอรี) อ่านฟอร์ม ตรวจลำดับรูป และให้ service ตรวจสิทธิ์สาขาหรือหาห้องก่อน แล้วค่อยเก็บไฟล์ · แยกการอ่านฟอร์มออกจาก SaveFromRequest ถ้าจำเป็น
- ขึ้นกับ: T-03
- DoD: T-03 เขียว · storage/asset_test.go และ room/images_test.go ยังเขียว · ตัดป้าย [ช่องว่าง] ไฟล์กำพร้าใน AC-14 · CHECK สะอาด

T-05 · ⚖ ต้องตัดสินใจ + Test: ผลของการแตะข้อมูลรายชิ้นข้ามสาขา (AC-4 ข้อ [ช่องว่าง])
- คำถาม: ตอนนี้อนุมัติ ปฏิเสธ และลบรูปสาขาได้ "ไม่พบข้อมูล" ส่วนดูใบจอง ยกเลิก ตั้งนัด แก้ เปลี่ยนสถานะ ลบห้อง อัปรูปห้อง ลบรูปห้อง ได้ "ไม่มีสิทธิ์" ควรเหมือนกันไหม
- ข้อเสนอ: ข้อมูลรายชิ้นได้ "ไม่พบข้อมูล" ทุกจุด ส่วนการขอดูรายการของสาขาอื่นยังได้ "ไม่มีสิทธิ์" เหมือนเดิม
- TC (หลังตัดสินใจและแก้ SPEC AC-4 แล้ว): admin สาขา A ทำทุกอย่างในลิสต์กับข้อมูลของสาขา B → ได้ผลแบบเดียวกันทั้งหมด
- ขึ้นกับ: การตัดสินใจ
- DoD: TEST(shared/access, TestCrossBranchStatus) แดง

T-06 · Implement: ใช้ผลข้ามสาขาแบบเดียวกันทุกจุด
- ทำ: ย้ายตัวแปลงผลข้ามสาขาที่อยู่ใน booking/service.go ไปไว้ที่ shared/access แล้วใช้กับทุก service ที่หาข้อมูลรายชิ้นก่อนตรวจสาขา (booking ดู ยกเลิก ตั้งนัด · room ทุกคำสั่งที่หาสาขาจากห้อง)
- ขึ้นกับ: T-05
- DoD: T-05 เขียว · access_test.go และ statemachine_test.go ยังเขียว · แก้ openapi ให้ตรง · CHECK สะอาด
