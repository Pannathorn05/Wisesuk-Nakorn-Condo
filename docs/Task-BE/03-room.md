# module room (T-32 – T-44)

ขอบเขตโค้ด: backend/internal/modules/room
อ้างอิง SPEC: AC-8, AC-12, AC-14, AC-19, AC-27
เทสที่มีแล้ว: amenities_test.go, closed_test.go, images_test.go

---

## เทสปิด AC

T-32 · Test: ค้นหาห้องว่าง (AC-12)
- ทำ: room/search_test.go
- TC
  - ห้องที่มีผู้เช่า ปิดปรับปรุง หรือถูกลบ → ไม่อยู่ในผล
  - มีใบรายวัน 10–12 → ค้น 11–13 ไม่เจอ · ค้น 12–14 เจอ · ระบุแค่วันเข้าพัก → เจอ
  - ใบที่ยกเลิกแล้วไม่กันห้อง
  - ค้นรายวันที่วันเข้าพักเท่ากับวันเช็คเอาท์ → ข้อมูลไม่ถูกต้อง
  - ไม่ระบุหน้า → หน้า 1 หน้าละ 20 · ขอหน้าละ 500 → ได้ 100 · หน้าเป็น 0 หรือ abc → คำขอผิดรูปแบบ
  - ประเภทไม่รู้จัก วันที่ผิดรูปแบบ ราคาเป็นตัวอักษร รหัสสาขาผิดตาราง → คำขอผิดรูปแบบ
  - ผลเรียงตามสาขาแล้วตามเลขห้อง
- ขึ้นกับ: —
- DoD: TEST(modules/room, TestSearchRooms) เขียว · ถ้าแดงเพราะโค้ดไม่ตรง SPEC ให้หยุดแล้วแตก task ใหม่

T-33 · Test: วงจรชีวิตของห้อง (AC-19)
- ทำ: room/lifecycle_test.go
- TC
  - admin เพิ่มห้องไม่ระบุสาขา → อยู่สาขาตัวเอง · super admin ไม่ระบุ → ถูกปฏิเสธให้ระบุสาขา
  - เลขห้องว่างหรือยาว 21 ตัว ราคา 0 ชั้น 0 ค่าน้ำติดลบ สถานะไม่รู้จัก → ข้อมูลไม่ถูกต้อง
  - ไม่ระบุสถานะ → ว่าง · ไม่ระบุอาคาร → อาคาร 1
  - เลขห้องซ้ำในสาขาเดียวกัน → "เลขห้องนี้มีอยู่ในสาขาแล้ว" · ซ้ำคนละสาขา → สำเร็จ
  - แก้ห้องไม่ส่งรูปปก → คงเดิม · ส่งสาขาอื่นมา → ถูกเมิน
  - เปลี่ยนสถานะได้ทุกทิศ · ลบ → หายจากรายการห้องของแอดมิน · ลบซ้ำ → สำเร็จ
  - ห้องคนละสาขา → ไม่มีสิทธิ์ · ห้องไม่มีจริง → ไม่พบข้อมูล
  - ทุกการกระทำมีบันทึกผูกกับสาขาของห้อง
- ขึ้นกับ: —
- DoD: TEST(modules/room, TestRoomLifecycle) เขียว

## แก้ช่องว่าง

T-34 · Test: ประเภทห้องที่ไม่มีจริงหรืออยู่คนละสาขา (AC-19 ข้อ [ช่องว่าง])
- TC: เพิ่มหรือแก้ห้องด้วยประเภทห้องที่ไม่มีจริง → ข้อมูลไม่ถูกต้องที่ช่องประเภทห้อง (ตอนนี้เกิดข้อผิดพลาดภายในระบบ) · ประเภทห้องของสาขาอื่น → ข้อมูลไม่ถูกต้อง
- ขึ้นกับ: —
- DoD: TEST(modules/room, TestRoomTypeMustBelongToBranch) แดง

T-35 · Implement: ตรวจประเภทห้องก่อนบันทึก
- ทำ: repository ตรวจว่าประเภทห้องเป็นของสาขานั้น แล้วใช้ในการเพิ่มและแก้ห้อง · ถ้าจะบังคับที่ฐานข้อมูลด้วย ให้ผูกประเภทห้องคู่กับสาขา
- ขึ้นกับ: T-34
- DoD: T-34 เขียว · ลบกรณีข้อผิดพลาดภายในระบบออกจาก openapi · ตัดป้ายใน SPEC AC-19 · CHECK สะอาด

T-36 · ⚖ ต้องตัดสินใจ + Test: ห้องที่ลบแล้วแก้ไม่ได้ (AC-19 ข้อ [ช่องว่าง])
- ข้อเสนอ: แก้ เปลี่ยนสถานะ ลบ อัปรูป เพิ่มหรือลบรูปแกลเลอรีของห้องที่ถูกลบ → ไม่พบข้อมูล
- TC: ลบห้องแล้วทำทั้ง 6 อย่าง → ไม่พบข้อมูลทุกอย่าง · ห้องในสาขาที่ปิดยังจัดการได้ตามปกติ
- ขึ้นกับ: การตัดสินใจ
- DoD: TEST(modules/room, TestDeletedRoomIsGone) แดง

T-37 · Implement: หาห้องฝั่งแอดมินเฉพาะห้องที่ยังไม่ถูกลบ
- ทำ: ตัวหาสาขาจากห้องใช้เฉพาะห้องที่ยังไม่ถูกลบ
- ขึ้นกับ: T-36
- DoD: T-36 เขียว · closed_test.go ยังเขียว · ตัดป้ายใน SPEC AC-19 · CHECK สะอาด

T-38 · Test: ลบรูปแกลเลอรีต้องตรงกับห้องที่ระบุ (AC-14 ข้อ [ช่องว่าง])
- TC: รูปเป็นของห้อง X → ลบโดยระบุห้อง Y ในสาขาเดียวกัน → ไม่พบข้อมูล และรูปยังอยู่
- ขึ้นกับ: —
- DoD: TEST(modules/room, TestDeleteImageMustMatchRoom) แดง

T-39 · Implement: ผูกการลบรูปกับห้องที่ระบุ
- ทำ: คำสั่งลบรูปแกลเลอรีเพิ่มเงื่อนไขห้อง
- ขึ้นกับ: T-38
- DoD: T-38 เขียว · images_test.go ยังเขียว · ตัดป้ายใน SPEC AC-14 · CHECK สะอาด

T-40 · ⚖ ต้องตัดสินใจ + Test: แก้ห้องที่มีใบจองค้าง (AC-19 และ AC-8 ข้อ [ช่องว่าง])
- ข้อเสนอ: เปลี่ยนประเภทการเข้าพักขณะมีใบที่ถือห้อง → "สถานะปัจจุบันทำรายการนี้ไม่ได้" · เปลี่ยนสถานะห้องยังทำได้ (แอดมินต้องปิดซ่อมได้)
- TC: ห้องรายวันมีใบรายวันค้าง → แก้เป็นรายเดือน → ถูกปฏิเสธ · ไม่มีใบค้าง → สำเร็จ
- ขึ้นกับ: การตัดสินใจ
- DoD: TEST(modules/room, TestStayTypeLockedByBookings) แดง

T-41 · Implement: กันเปลี่ยนประเภทการเข้าพักของห้องที่มีใบค้าง
- ทำ: service แก้ห้องตรวจใบที่ถือห้องก่อนเปลี่ยนประเภท (ในธุรกรรมเดียวกับการแก้)
- ขึ้นกับ: T-40
- DoD: T-40 เขียว · ตัดป้ายใน SPEC AC-19 และข้อใบรายวันไม่กันใบรายเดือนใน AC-8 · CHECK สะอาด

T-42 · Test: ค้นหาใช้กติกาใบรายเดือนเดียวกับตอนจอง (AC-12 ข้อ [ช่องว่าง])
- TC: ห้องมีใบรายเดือนเข้าอยู่อีก 30 วัน → ค้นโดยไม่ระบุวันเข้าอยู่ → ไม่เจอห้องนี้
- ขึ้นกับ: —
- DoD: TEST(modules/room, TestSearchMonthlyMatchesBooking) แดง

T-43 · Implement: ตัดห้องที่มีใบรายเดือนถืออยู่ทุกกรณี
- ทำ: เงื่อนไขค้นหาตัดห้องที่มีใบรายเดือนที่ถือห้องโดยไม่ดูวันเข้าอยู่ ให้ตรงกับตอนจอง (ถ้า human อยากให้เห็นห้องที่ว่างถึงวันเข้าอยู่ ต้องแก้ทั้งค้นหาและตอนจองพร้อมกันแทน)
- ขึ้นกับ: T-42
- DoD: T-42 เขียว · T-32 ยังเขียว · แก้ SPEC AC-12 · CHECK สะอาด

T-44 · ⚖ ต้องตัดสินใจ: ดูรายละเอียดห้องที่ไม่ว่าง (AC-27 ข้อ [ช่องว่าง])
- ตอนนี้: ห้องที่มีผู้เช่าหรือปิดปรับปรุงยังดูรายละเอียดได้ปกติ
- ทางเลือก: คงไว้ให้หน้าเว็บดูสถานะเอง หรือเพิ่มข้อมูลบอกว่าจองได้หรือไม่
- ข้อเสนอ: คงไว้ แล้วแก้ SPEC ตัดป้าย [ช่องว่าง]
- ขึ้นกับ: การตัดสินใจ
- DoD: SPEC ถูกแก้ตามที่ตัดสินใจ · ถ้าเลือกเพิ่มข้อมูล ให้แตก task Test และ Implement ใหม่
