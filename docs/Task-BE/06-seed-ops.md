# ข้อมูลตั้งต้น การตั้งค่า และเอกสาร (T-72 – T-77)

ขอบเขต: backend/cmd/seed, backend/.gitignore, backend/.env.example, backend/docker-compose.yml, backend/README.md, docs/TASKS.md, docs/PLAN.md
อ้างอิง SPEC: AC-17, AC-24, หัวข้อข้อมูลตั้งต้น, หัวข้อเอกสารที่ไม่ตรงกับโค้ด
เทสที่มีแล้ว: cmd/seed/images_test.go

---

T-72 · Implement: สลิปจริงต้องไม่เข้า git (AC-17 ข้อ [ช่องว่าง])
- ทำ: backend/.gitignore กันโฟลเดอร์สลิป โดยไม่กันโฟลเดอร์รูปเดโมของสาขา · ตรวจว่ามีสลิปหลุดเข้า repo แล้วหรือยัง ถ้ามีให้แจ้ง human ก่อนเอาออก
- ขึ้นกับ: —
- DoD: git check-ignore ของไฟล์ในโฟลเดอร์สลิปคืน path · ของไฟล์ในโฟลเดอร์รูปเดโมไม่คืนอะไร · ตัดป้ายใน SPEC AC-17

T-73 · Test: seed บนฐานข้อมูลที่มีผู้ดูแลชุดเก่า (หัวข้อข้อมูลตั้งต้น ข้อ [ช่องว่าง] [ยังไม่ยืนยัน])
- ทำ: cmd/seed/seed_test.go
- TC
  - มี admin.1@wisetsuk.com ดูแลประชาอุทิศ 45 อยู่แล้ว → รัน seed → ไม่ล้ม และสาขานั้นยังมีผู้ดูแลคนเดียว
  - มี super admin อีเมลอื่นอยู่แล้ว → รัน seed → ไม่ล้ม และยังมี super admin คนเดียว
  - รัน seed สองครั้งติดกันบนฐานข้อมูลเปล่า → ไม่ล้ม และจำนวนข้อมูลเท่าเดิม
- ขึ้นกับ: —
- DoD: cd backend && go test ./cmd/seed/... -run TestSeedIdempotent -race -count=1 → สองข้อแรกแดง (ยืนยันข้อที่ยังไม่ยืนยันใน SPEC)

T-74 · Implement: seed ข้ามสาขาหรือตำแหน่งที่มีคนดูแลแล้ว
- ทำ: เพิ่ม admin เฉพาะเมื่อสาขานั้นยังไม่มีผู้ดูแล และเพิ่ม super admin เฉพาะเมื่อยังไม่มี พร้อมพิมพ์เตือนว่าข้ามบัญชีไหน
- ขึ้นกับ: T-73
- DoD: T-73 เขียว · images_test.go ยังเขียว · แก้หัวข้อข้อมูลตั้งต้นใน SPEC · CHECK สะอาด

T-75 · Implement: ไฟล์ตัวอย่าง env ใช้งานได้ทันที (AC-24 ข้อ [ช่องว่าง])
- ทำ: รหัสของ Google และ Facebook เป็นค่าว่าง · หน้าที่รับผลการล็อกอินชี้ localhost:3000/auth/callback · เพิ่มรหัสตั้งต้นของผู้ดูแลพร้อมคำอธิบาย · docker compose ส่งค่าความแรงการเข้ารหัสและรหัสตั้งต้นของผู้ดูแลเข้า backend
- ขึ้นกับ: —
- DoD: คัดลอกไฟล์ตัวอย่างเป็น .env แล้ว build backend ใหม่ → รายชื่อผู้ให้บริการล็อกอินภายนอกว่าง · ตัดป้ายใน SPEC AC-24

T-76 · เอกสาร: backend/README.md ให้ตรงกับโค้ด
- ทำ: แก้ตามหัวข้อ "เอกสารที่ไม่ตรงกับโค้ด" ใน SPEC
  - ตารางบัญชีตั้งต้นใช้อีเมล admin ชุดใหม่ และเพิ่ม member 3 บัญชี
  - เอาเรื่องลืมรหัสผ่าน ระบบส่งอีเมล และ token รีเซ็ตออก จนกว่า T-22 เสร็จ
  - แผนภาพสถานะการจองเหลือ 4 สถานะ (ปฏิเสธแล้วกลับเป็นรอชำระเงิน)
  - ข้อ super admin เลือกสาขา, คำสั่งแก้ข้อมูลผูกสาขาเสมอ, ปิดสาขาแล้วปิดเลย ให้ตรงกับผลของ T-71, T-58, T-48
  - เพิ่มช่องทางล็อกอินด้วย Google/Facebook และช่องทางไฟล์บนดิสก์
- ขึ้นกับ: ทำรอบแรกได้ทันที แล้วแก้ซ้ำหลัง T-48, T-58, T-71
- DoD: ทุกช่องทางในตาราง README ตรงกับ docs/openapi.yaml · human รีวิว

T-77 · เอกสาร: ปิด docs/TASKS.md และแก้ docs/PLAN.md
- ทำ: หัว docs/TASKS.md เขียนว่างาน backend ย้ายมาที่ docs/Task-BE และเลข T-XX ในไฟล์เก่าไม่ใช้แล้ว (เลขชุดนี้แทน) · แก้จำนวนรหัสข้อผิดพลาดเป็น 13 · docs/PLAN.md แก้เรื่อง SQLite ให้ตรงกับที่ใช้ PostgreSQL
- ขึ้นกับ: —
- DoD: human รีวิว · ไม่มีเลข T-XX ที่ความหมายชนกันค้างอยู่สองที่
