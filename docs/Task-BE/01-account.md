# module account (T-07 – T-22)

ขอบเขตโค้ด: backend/internal/modules/account, internal/auth, internal/oauth
อ้างอิง SPEC: AC-1, AC-2, AC-3, AC-5, AC-18, AC-23, AC-24, AC-29
เทสที่มีแล้ว: oauth_test.go, ratelimit_test.go, revocation_test.go, staff_test.go

---

## เทสปิด AC ที่ยังไม่มีเทส

T-07 · Test: สมัครสมาชิก (AC-1)
- ทำ: account/register_test.go
- TC
  - ข้อมูลครบ → สำเร็จ ได้การล็อกอินในบทบาท member และมีบันทึกการสมัคร
  - แนบบทบาทมาด้วย → ข้อมูลไม่ถูกต้อง "ไม่อนุญาตให้ส่งฟิลด์นี้"
  - สมัคร A@B.com หลังมี a@b.com → "อีเมลนี้ถูกใช้งานแล้ว" · อีเมลซ้ำกับผู้ดูแลที่ถูกลบ → ข้อมูลไม่ถูกต้อง
  - ผิด 3 ช่องพร้อมกัน → บอกครบ 3 ช่อง
  - รหัส 7 ตัว, 73 ตัว, ตัวอักษรล้วน, ตัวเลขล้วน → ข้อมูลไม่ถูกต้องที่ช่องรหัสผ่าน
  - เบอร์ว่างหรือ abc → ข้อมูลไม่ถูกต้อง · ชื่อยาว 101 ตัวอักษร → ข้อมูลไม่ถูกต้อง
  - ข้อมูลอ่านไม่ออก → คำขอผิดรูปแบบ
  - สมัครอีเมลเดียวกันสองครั้งพร้อมกัน → สำเร็จหนึ่ง อีกครั้งถูกปฏิเสธ และไม่มีข้อผิดพลาดภายในระบบ
- ขึ้นกับ: —
- DoD: TEST(modules/account, TestRegister) เขียว · ถ้าแดงเพราะโค้ดไม่ตรง SPEC ให้หยุดแล้วแตก task ใหม่

T-08 · Test: อายุและการหมุนเวียนของการล็อกอิน (AC-3)
- ทำ: account/token_test.go
- TC
  - ต่ออายุด้วยใบที่ใช้ได้ → ได้คู่ใหม่ · ใช้ใบเดิมซ้ำ → ถูกปฏิเสธ
  - ต่ออายุใบเดียวกันพร้อมกันสองครั้ง → สำเร็จหนึ่ง ถูกปฏิเสธหนึ่ง
  - ใบหมดอายุ ใบมั่ว ใบว่าง → ถูกปฏิเสธ
  - ออกจากระบบแล้วต่ออายุใบนั้น → ถูกปฏิเสธ · ออกจากระบบด้วยค่ามั่ว → สำเร็จ · ไม่ส่งข้อมูลมา → คำขอผิดรูปแบบ
  - เปลี่ยนรหัสผ่าน → ต่ออายุทุกใบของบัญชีไม่ได้ รวมใบปัจจุบัน · access token ใบเดิมยังใช้ได้
  - access token ปลอม ลงนามวิธีอื่น ออกโดยระบบอื่น หรือหมดอายุ → ถูกปฏิเสธ
  - ระงับแล้วเปิดคืน → access token ใบเดิมกลับมาใช้ได้
  - ฐานข้อมูลไม่มี refresh token ตัวจริง มีแต่ค่า hash
- ขึ้นกับ: —
- DoD: TEST(modules/account, TestToken) เขียว

T-09 · Test: แจ้งเตือนของตัวเอง (AC-5)
- ทำ: account/notifications_test.go
- TC
  - มีแจ้งเตือน 35 รายการ ยังไม่อ่าน 33 → ได้ 30 รายการล่าสุด และจำนวนที่ยังไม่อ่านเป็น 33
  - กดอ่านทั้งหมด → จำนวนที่ยังไม่อ่านเป็น 0
  - กดอ่านรายการของตัวเอง → อ่านรายการเดียว · กดอ่านรายการของคนอื่น → สำเร็จ แต่ของคนอื่นยังไม่ถูกอ่าน
  - ไม่ส่งข้อมูลมา หรือรหัสแจ้งเตือนผิดรูปแบบ → คำขอผิดรูปแบบ
  - เอารหัสที่ได้จากรายการแจ้งเตือนไปกดอ่านตรง ๆ → สำเร็จ (TC นี้แดงจนกว่า T-10 เสร็จ)
- ขึ้นกับ: —
- DoD: ทุก TC เขียวยกเว้นข้อสุดท้ายที่แดงเพราะรหัสขึ้นต้นด้วย usr

T-10 · Implement: รหัสแจ้งเตือนขึ้นต้นด้วย ntf (AC-5 ข้อ [ช่องว่าง])
- ทำ: account/model.go เปลี่ยนชนิดรหัสของแจ้งเตือนเป็นรหัสแจ้งเตือน · ลบหมายเหตุช่องว่างใน openapi · ตัดป้าย [ช่องว่าง] ใน SPEC AC-5
- ขึ้นกับ: T-09
- DoD: T-09 เขียวครบทุก TC · CHECK สะอาด

T-11 · Test: โปรไฟล์และเปลี่ยนรหัสผ่าน (AC-29)
- ทำ: account/profile_test.go
- TC
  - admin ดูโปรไฟล์ → มีสาขา ธงต้องเปลี่ยนรหัส และไม่มีรหัสผ่าน
  - แก้โปรไฟล์โดยไม่ส่งรูป → รูปเดิมคงอยู่ · ไม่ส่งเบอร์ → ข้อมูลไม่ถูกต้อง · มีบันทึกการแก้โปรไฟล์
  - เปลี่ยนรหัสโดยไม่กรอกรหัสเดิม → ข้อมูลไม่ถูกต้อง · รหัสเดิมผิด → "รหัสผ่านเดิมไม่ถูกต้อง" · ใหม่ซ้ำเดิม → "รหัสผ่านใหม่ต้องไม่ซ้ำกับรหัสผ่านเดิม" · ยืนยันไม่ตรง → "รหัสผ่านยืนยันไม่ตรงกัน"
  - สำเร็จ → "เปลี่ยนรหัสผ่านเรียบร้อยแล้ว กรุณาเข้าสู่ระบบใหม่" ธงต้องเปลี่ยนรหัสถูกปลด และมีบันทึก
- ขึ้นกับ: —
- DoD: TEST(modules/account, TestProfile) เขียว

## แก้ช่องว่าง

T-12 · ⚖ ต้องตัดสินใจ + Test: ตรวจรูปแบบรูปโปรไฟล์ (AC-29 ข้อ [ช่องว่าง])
- ข้อเสนอ: ใช้กติกาเดียวกับช่องรูปอื่น (http หรือ https ปล่อยว่างได้)
- TC: ลิงก์ javascript, data หรือ path ลอย ๆ → ข้อมูลไม่ถูกต้องที่ช่องรูปโปรไฟล์ · ลิงก์ https → สำเร็จ · ค่าว่าง → สำเร็จ (ล้างรูป)
- ขึ้นกับ: การตัดสินใจ
- DoD: TEST(modules/account, TestAvatarURL) แดง

T-13 · Implement: ตรวจรูปแบบรูปโปรไฟล์
- ทำ: service แก้โปรไฟล์ใช้ตัวตรวจลิงก์รูปแบบเดียวกับช่องรูปอื่น · แก้ openapi และ SPEC AC-29
- ขึ้นกับ: T-12
- DoD: T-12 เขียว · CHECK สะอาด

T-14 · Test: ย้ายผู้ดูแลไปสาขาที่ไม่มีจริง (AC-18 ข้อ [ช่องว่าง])
- TC: super admin แก้ admin ให้ย้ายไปสาขาที่ไม่มีจริง → "ไม่พบสาขาที่เลือก" (ตอนนี้เกิดข้อผิดพลาดภายในระบบ)
- ขึ้นกับ: —
- DoD: TEST(modules/account, TestUpdateStaffUnknownBranch) แดง

T-15 · Implement: ตรวจว่าสาขามีจริงตอนแก้ผู้ดูแล
- ทำ: การแก้ผู้ดูแลตรวจสาขาแบบเดียวกับตอนเพิ่ม (เฉพาะเป้าหมายที่เป็น admin)
- ขึ้นกับ: T-14
- DoD: T-14 เขียว · staff_test.go ยังเขียว · ลบกรณีข้อผิดพลาดภายในระบบออกจาก openapi · ตัดป้ายใน SPEC AC-18 · CHECK สะอาด

T-16 · Test: จำกัดความถี่ล็อกอินเมื่อยิงพร้อมกัน (AC-2 ข้อ [ยังไม่ยืนยัน])
- TC: ยิงล็อกอินรหัสผิด 30 ครั้งพร้อมกันจากอีเมลและ IP เดียวกัน → ได้ข้อความรหัสผิดไม่เกิน 5 ครั้ง ที่เหลือถูกจำกัด
- ขึ้นกับ: —
- DoD: เขียว → ตัดป้าย [ยังไม่ยืนยัน] ใน SPEC · แดง → human ตัดสินว่ายอมรับ หรือแตก task ล็อกการนับราย IP

T-17 · Test: เวลาตอบเมื่ออีเมลไม่มีเทียบกับรหัสผิด (AC-2)
- TC: จับเวลาอย่างละ 50 ครั้ง ค่ากลางต่างกันไม่เกิน 20%
- ทำ: แยก build tag หรือ -run เฉพาะ เพราะช้าและไวต่อเครื่อง ไม่รวมในชุดเทสปกติ
- ขึ้นกับ: —
- DoD: เทสเขียวเมื่อรันแยก · ระบุวิธีรันไว้ในคอมเมนต์หัวไฟล์

T-18 · ⚖ ต้องตัดสินใจ + Test: ล็อกอิน Google/Facebook ด้วยอีเมลของบัญชีที่ถูกลบ (AC-24 ข้อ [ช่องว่าง])
- ตอนนี้: หน้าเว็บได้ข้อความ "ข้อมูลนี้มีอยู่ในระบบแล้ว"
- ข้อเสนอ: ข้อความเฉพาะ "อีเมลนี้เคยถูกใช้กับบัญชีที่ถูกลบแล้ว กรุณาติดต่อผู้ดูแลระบบ"
- TC: ผู้ดูแลถูกลบแล้วล็อกอินด้วย Google อีเมลเดียวกัน → ได้ข้อความที่ตกลง และไม่มีบัญชีหรือการผูกบัญชีใหม่
- ขึ้นกับ: การตัดสินใจ
- DoD: เทสใช้ผู้ให้บริการปลอมแบบเดียวกับ oauth_test.go · แดง

T-19 · Implement: ข้อความเฉพาะสำหรับอีเมลของบัญชีที่ถูกลบ
- ทำ: ตรวจอีเมลซ้ำก่อนสร้างบัญชีใหม่ในขั้นจับคู่บัญชี แล้วคืนข้อความที่ตกลง
- ขึ้นกับ: T-18
- DoD: T-18 เขียว · oauth_test.go ยังเขียว · ตัดป้ายใน SPEC AC-24 · CHECK สะอาด

## ฟีเจอร์ใหม่: ลืมรหัสผ่าน (AC-23 [ยังไม่ implement])

T-20 · ⚖ ต้องตัดสินใจ + Migration: ที่เก็บลิงก์รีเซ็ตและระบบส่งอีเมล
- ต้องตัดสินใจ: ผู้ให้บริการส่งอีเมล, ชื่อค่าตั้งค่าใน env, ที่อยู่หน้าเว็บที่ลิงก์ชี้ไป
- ทำ: migration ใหม่เก็บผู้ใช้ ค่า hash ของลิงก์ เวลาหมดอายุ เวลาที่ใช้แล้ว และ index สำหรับนับรายชั่วโมง · package mailer ที่เทสใส่ของปลอมได้ · โหมด production ที่ไม่ได้ตั้งระบบส่งอีเมล → ไม่ยอมเริ่มทำงาน
- ขึ้นกับ: การตัดสินใจ
- DoD: migration รันซ้ำได้ · CHECK สะอาด

T-21 · Test: ขอลิงก์และตั้งรหัสใหม่
- TC (ตาม AC-23)
  - ขอลิงก์ด้วยอีเมลที่มี ไม่มี และถูกระงับ → ตอบเหมือนกันทุกกรณี · ส่งอีเมลเฉพาะบัญชีที่ใช้งานได้
  - เวลาตอบของอีเมลที่มีกับไม่มีต่างกันไม่เกิน 20%
  - ขอครั้งที่ 6 ภายในหนึ่งชั่วโมง → ตอบเหมือนเดิมแต่ไม่มีอีเมลออก
  - ขอใหม่ → ลิงก์เก่าใช้ไม่ได้ · ลิงก์เกิน 15 นาทีหรือใช้แล้ว → ข้อมูลไม่ถูกต้อง
  - ตั้งรหัสใหม่สำเร็จ → ล็อกอินด้วยรหัสใหม่ได้ ต่ออายุด้วย refresh token เดิมไม่ได้ และมีบันทึกการรีเซ็ตรหัสผ่าน
- ขึ้นกับ: T-20
- DoD: TEST(modules/account, TestPasswordReset) แดง

T-22 · Implement: ขอลิงก์และตั้งรหัสใหม่
- ทำ: handler service repository และช่องทางสาธารณะสองช่อง · เพิ่มการกระทำ "รีเซ็ตรหัสผ่าน" ในประวัติ · เพิ่มใน openapi และ README · ตัดป้าย [ยังไม่ implement] ใน SPEC AC-23 · แก้หน้าเว็บให้ลิงก์ "ลืมรหัสผ่าน?" ชี้หน้าใหม่ (แจ้งทีม frontend)
- ขึ้นกับ: T-21
- DoD: T-21 เขียว · ALL เขียว · CHECK สะอาด
