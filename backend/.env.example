APP_ENV=development
PORT=8080

POSTGRES_USER=wisetsuk
POSTGRES_PASSWORD=dev_local_pw_2026
POSTGRES_DB=wisetsuk

DATABASE_URL=postgres://wisetsuk:dev_local_pw_2026@localhost:5432/wisetsuk?sslmode=disable

JWT_SECRET=dev_only_change_me_0123456789abcdefghijklmnop
ACCESS_TOKEN_TTL_MIN=30
REFRESH_TOKEN_TTL_DAY=30
BCRYPT_COST=12

UPLOAD_DIR=./uploads
PUBLIC_BASE_URL=http://localhost:8080
MAX_UPLOAD_MB=5

ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# IP หรือ CIDR ของ reverse proxy ที่อยู่หน้า API (nginx, load balancer) คั่นด้วยจุลภาค
# ระบบเชื่อ X-Forwarded-For เฉพาะคำขอที่มาจากในรายการนี้ — เว้นว่างถ้าไม่มี proxy
# ตัวอย่าง: TRUSTED_PROXIES=10.0.0.5,172.16.0.0/12
TRUSTED_PROXIES=

SEED_DEFAULT_PASSWORD=Wisetsuk!2026

# ---------------------------------------------------------------- pgAdmin
# เข้าใช้งานที่ http://localhost:5050 (เปิดเฉพาะจากเครื่องนี้เท่านั้น)
PGADMIN_EMAIL=admin@wisetsuk.com
PGADMIN_PASSWORD=dev_pgadmin_pw_2026
PGADMIN_PORT=5050

GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
FRONTEND_OAUTH_CALLBACK_URL=http://localhost:8080/health